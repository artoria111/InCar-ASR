# Model artifacts

Large model, token, CMVN, audio, and device profiling files are intentionally
not stored in normal Git history.

Place local artifacts here and record their source plus SHA256 in
`artifact_manifest.json`. Copy `artifact_manifest.example.json` as a starting
point.
